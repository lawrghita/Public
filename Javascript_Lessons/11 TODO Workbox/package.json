{
  "name": "workbox-TODO",
  "version": "1.0.0",
  "description": "TODO with service worker setup by workbox",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy": "copyfiles -u 1 src/**/**/* src/**/* src/* build",
    "build": "npm run copy  && workbox injectManifest workbox-config.js",
    "start": "node server.js"
  },
  "author": "lawrghita",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "global": "^4.4.0"
  },
  "devDependencies": {
    "copyfiles": "^1.2.0",
    "workbox-cli": "^6.5.4"
  }
}
